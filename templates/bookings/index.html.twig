{% extends 'base.html.twig' %}

{% block title %}ChargeIT My Bookings{% endblock %}

{% block body %}
    {% include 'navbar.html.twig' %}
    <script>
        function deleteBooking(plate) {
            const choice = confirm("Are you sure you want to cancel this booking?");
            if(choice)
                location.href = "/booking/" + plate + "/delete";
        }
        function newPopup(url, title) {
            window.open(url, title, 'scrollbars=no,resizable=no,status=no,location=no,toolbar=no,menubar=no,width=450,height=320')
        }
    </script>

    <style>
        .example-wrapper { margin: 1em auto; max-width: 75%; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        table, th, td {
            font-size: 1em;
            border: 1px solid black;
            text-align: center;
        }
    </style>

    <div class="example-wrapper">
        <div style="text-align: center">
            <h1>My Bookings</h1>
            <h3>Here's a list with all of your bookings:</h3>
        </div>
        <div>
            <input disabled="disabled" type="button" value="Create a booking" onclick="newPopup('{{ path('app_booking_create') }}', 'create_booking')" style="cursor: not-allowed ;font-size: 1.08em; width: 100%; height: 2em; margin-bottom: 0.5rem" />
            <table style="width: 100%">
                <colgroup>
                    <col style="width: 15%">
                    <col style="width: 7%">
                    <col style="width: 10%">
                    <col style="width: 10%">
                    <col style="width: 5%">
                    <col style="width: 15%">
                </colgroup>
                <tr>
                    <th>Station</th>
                    <th>Plug</th>
                    <th>Car</th>
                    <th>Date & Time</th>
                    <th>Duration</th>
                    <th>Options</th>
                </tr>
{#                {% for obj in jsonData %}#}
{#                    <tr>#}
{#                        <td>{{ obj.plate }}</td>#}
{#                        <td>{{ obj.plug }}</td>#}
{#                        <td>#}
{#                            <a href="javascript:deleteCar('{{ obj.plate }}')">Delete</a>#}
{#                        </td>#}
{#                    </tr>#}
{#                {% endfor %}#}
            </table>
        </div>
    </div>
{% endblock %}
